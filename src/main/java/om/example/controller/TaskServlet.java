package om.example.controller;

/*
 * このファイルは誤って配置されていた重複サーブレットのプレースホルダです。
 * 実際の `TaskServlet` は `com.example.controller` パッケージに存在します。
 *
 * 元の重複クラスに含まれていた `@WebServlet` アノテーションや実装は
 * ここでは削除/無効化されています。これにより、コンテナによる二重登録を防ぎます。
 */

// noop placeholder - intentionally empty
